services:
  eduarda-clinica:
    build:
      context: ..
      dockerfile: deploy_vps/Dockerfile
    container_name: eduarda_clinica_app
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    networks:
      - web

  nginx:
    image: nginx:alpine
    container_name: eduarda_clinica_nginx
    restart: unless-stopped
    depends_on:
      - eduarda-clinica
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
    networks:
      - web

  certbot:
    image: certbot/certbot
    container_name: eduarda_clinica_certbot
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: /bin/sh -c
    command: "trap exit TERM; while :; do sleep 12h & wait $${!}; certbot renew; done"

networks:
  web:
    driver: bridge
